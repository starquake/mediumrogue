# Copy to .env for local overrides; the Makefile auto-includes it.
# All values shown are the defaults.

#LISTEN_ADDR=:8080

# World-turn period. Shrink for fast local iteration (e.g. 500ms).
#TURN_INTERVAL=5s

# SSE idle keep-alive comment cadence.
#HEARTBEAT_INTERVAL=15s
